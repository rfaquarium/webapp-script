{
  "name": "rf-workspace-pro",
  "version": "1.0.0",
  "description": "Rich Fish Aquarium Operations & Workshop Operating System",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "esbuild speed-insights.js --bundle --outfile=speed-insights.bundle.js --format=iife",
    "lint": "echo 'No linter configured for static HTML'"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
